name: Weekly Log Analysis

on:
  schedule:
    - cron: "0 3 * * 1" # every Monday at 03:00 UTC
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install Python and dependencies
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install pandas matplotlib seaborn user-agents pytz

      - name: Check API key presence
        run: |
          if [ -z "$API_KEY" ]; then
            echo "API_KEY is empty"
            exit 1
          fi
          echo "API_KEY length: ${#API_KEY}"
        env:
          API_KEY: ${{ secrets.LOG_API_KEY }}

      - name: Fetch log file
        env:
          API_KEY: ${{ secrets.LOG_API_KEY }}
        run: |
          curl "https://loucantou.yvelin.net/logs/loucantou-access.log?api_key=$API_KEY" -o loucantou-access.log

      - name: Run log analyzer
        run: |
          python analyse-traffic.py --period w --logpath loucantou-access.log

      - name: Commit and push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Weekly log analysis update"
            git push
          fi
